import random

class ordenacion_tareas:
    def __init__(self,tareas):
        self.tareas = defaultdict(tareas) 
        self.T = tareas
    def ordenacio_tareas(tareas):
      for j in range(len(tareas)):
  